const config = {
  // // 启动端口
  // port: 3000,

  // 数据库配置
  database: {
    DATABASE: 'teaching-evaluation', // 数据库名称
    USERNAME: 'root', //mysql用户名
    PASSWORD: 'root', //mysql密码
    PORT: '3306',     //mysql端口号
    HOST: '127.0.0.1' //服务器ip (localhost)
  }
}

module.exports = config